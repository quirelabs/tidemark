# @quirelabs/tidemark

## 0.0.1

First release.

- `tidemark snapshot`, `diff`, `report` and `branch`
- TypeScript config loading with no loader dependency
- Terminal, markdown and JSON output, with values treated as untrusted input
- Exit codes that distinguish a tool failure from a `--fail-on` threshold
