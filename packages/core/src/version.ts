/** Lowest Postgres major Tidemark supports. Not negotiable. */
export const MIN_POSTGRES_MAJOR = 15;

/** Kept in step with the package version, and stamped into every artifact. */
export const TIDEMARK_VERSION = "0.0.1";
